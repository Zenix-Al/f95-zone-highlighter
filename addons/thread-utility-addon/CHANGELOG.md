# Changelog

## v1.0.1 - Core UI alignment

- Aligned the thread palette, controls, tags, prefixes, and settings dialog with the core Settings palette and semantic user-tag colors.
- Replaced text disclosure arrows with accessible CSS chevrons and made the settings editor substantially more compact on desktop and mobile.
- Fixed dialog scroll ownership so the core host no longer adds a redundant scrollbar while the intended content region remains scrollable.
- Added Playwright visual coverage for desktop, narrow, disclosure, tag-color, settings-footer, and core-reference states.

## v1.0.0 - Initial release

- Initial release.
